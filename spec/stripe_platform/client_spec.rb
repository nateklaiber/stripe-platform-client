# frozen_string_literal: true

RSpec.describe StripePlatform::Client do
  it "has a version number" do
    expect(StripePlatform::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
